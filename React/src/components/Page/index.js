import React from 'react';
import { Route } from 'react-router-dom';

import Header from './Header';

import './page.scss';

const Page = () => (
  <main className="page">
    <Header />
  </main>
);

export default Page;
